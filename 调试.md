## S1. 应用开发

### 调试面板

#### 1. 统一调试面板（应用级）

> [!IMPORTANT]
>
> **设计考量**：调试面板
>
> **整体结构**：**目标调试对象** <= WebSocket => **后端服务** <= WebSocket => **调试面板**
>
> **面板接入**：此功能模块作为 微服务子应用，通过 micro-app 接入到资源树的主应用（基座），并通过 globalData 与基座进行通信
>
> > 1. 调试面板 与 目标调试对象 建立映射关系
> > 2. 可以考虑 emit 或者其他方式 来实现，但目前仍以 globalData 为主要实现方式，需考虑异步，丢失，多次发送的问题
>
> **通信协议**：设计一套协议来实现目标调试对象与调试面板之间的数据通信，也可以基于 **Chrome DevTools Protocol (CDP)** 实现
>
> > 先看看公司是否已有该方面的功能实现
>
> **前端实现**：
>
> * 伸缩控制面板：当前工程中已编写了 ResizeSplitPane，但只实现了横向，可拓展纵向（亦可选用第三方库，比如: splitpanes)
>
> * 资源文件列表：使用 el-tree-select 来实现
>
> * 文件内容显示：使用 monaco-editor (-vue3)，或者基于公司已有的 SDK 来实现断点调试等功能
>
> * 断点存储：可以使用 localStorage 来存储，但是需要明确哪些数据需缓存，需要缓存多久，什么时候清空缓存
>
>   > 1. 若是缓存的断点过多，考虑 IndexDB 来接管存储，采用 LRU 来管理常用有限的断点存储（以应用为单位）
>   > 2. 注意 key 的定义，需要以 用户 + 应用 作为唯一标识，同时定义出数据存储格式
>
> * 选项卡：可以使用 ino-tabs 实现
>
> * 数据表格：可以使用  el-table 实现（亦或可以使用第三方库 vxe-table）
>
> **服务通信**：使用 WebSocket 来实现，收发调试指令绑定的通信数据（符合通信协议要求的数据）
>
> > 需要新建，不能与系统中已有一套 WebSocket 共用
>
> **自定义文件**：若是采用了 CDP，但可能存在其中未实现的文件，需要进行扩展
>
> * 将原文件 **编译/转换**  成可执行的 JavaScript 代码文件，同时生成对应的 Source Map 文件，通过 Vite Plugin 来实现
>
> * 语法解析：将文件内容转换成一种程序可以理解的结构，即抽象语语法树 AST
>
>   > Web页面编辑器:
>   >
>   > 移动端页面编辑器：
>   >
>   > 服务逻辑：
>   >
>   > 服务脚本：
>
> * 代码转换与生成：生成可执行的 JavaScript 代码
>
>   > Babel：@babel/traverse（遍历和修改 AST）；@babel/generator（生成 JavaScript 代码和 Source Map）
>   >
>   > magic-string
>
> * 编写 Vite 插件，即使用 transform 钩子来完成

#### 2. 资源列表

- 动作
- 事件逻辑
- 服务逻辑：支持断点调试
- 服务脚本：支持断点调试

### ~~公式编辑器~~

### 日志

> [!IMPORTANT]
>
> **设计考量**：此日志仅是 **此次调试过程 **的日志信息，不做持久化，因此前端仅需要被动接受服务端推送日志数据即可
>
> **面板接入**：此功能模块作为 微服务子应用，通过 micro-app 接入到资源树的主应用（基座），并通过 globalData 与基座进行通信
>
> > 需要将现有的 globalData 数据格式进行拓展，增加类型 type, timestamp 以及日志的 payload 数据格式
>
> **实现方式**：日志目前仅需显示，可以编写自定义组件 + 虚拟滚动列表来实现（若需要高级功能，可用 monaco-editor-vue3）
>
> **数据存储**：目前考虑直接存入内存（格式化好的原始日志数据），视窗展示的数据从此中截取（拷贝），不允许破坏原数据
>
> **服务通信**：打开日志面板的时候，建立通信连接，采用 EventSource (SSE) 的方式接受日志服务推送日志数据流，以用于展示
>
> > 1. 连接建立成功之前，若已有调试日志产生，服务需要将这些日志作缓存，当通信成功之后，传输给页面，可不再保留
> > 2. 每次调试都需要一个唯一的会话ID，因为存在多人操作同一资源调试的情况（？是否需要，看具体需求？）
> > 3. 定义日志格式
> >    * 类型: 属于哪个资源模块
> >    * 分级: 普通 & 错误 ...
> >    * 时间: 发生时间
> >    * 错误详情：错误的详细描述信息
> >    * 所属来源：便于日志跳转至其对应的资源文件
>
> **其他功能**：
>
> - **日志搜索**：基于前端存储的日志搜索，不筛选，符合搜索条件的词条或者日志 高亮显示，并提供向上｜向下跳转的功能
>
>   > 视窗滚动：截取符合要求的原始日志数据（拷贝），基于搜索条件对即将显示的日志做 搜索标识处理后显示
>
> - **日志跳转（若要）**：可通过 globalData 通信方式，来实现跳转到对应的原始文件，需考虑日志 payload 的数据格式
>
> **性能优化：**以实现功能为主



























### 

## S2. 沙箱调试

### 调试面板

#### 1. 统一调试面板（应用级）

#### 2. 资源列表

- 动作
- 事件逻辑
- 服务逻辑：支持断点调试
- 服务脚本：支持断点调试（？）

## S3. 运行时调试

### 日志

=> microapp 接入调试面版



### 资源运行监控