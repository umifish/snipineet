# AI Agent 协作规范（总纲）

> 本文件是 `supernova` monorepo 内 **AI 必须遵守** 的协作总纲：约束、执行准则、多 Agent 分工与上下文管理。  
> 工程落地细节（编码、lint、构建、目录细则）以 `docs/ENGINEERING_CAPABILITIES.md` 与可执行配置为准；**落地操作顺序与命令组合**以 `docs/AI_OPERATION_PLAYBOOK.md` 为准。

**动态上下文加载**：多份文档交叉引用时，勿一次性假定「已读全文」。按任务**按需打开**相关章节，细则见 **第 8.1 节**；**禁止**仅凭文件名或标题臆测条文内容。

## 1. 目的与适用范围

- **目的**：在可验证、可审计、小步交付的前提下完成变更；禁止用猜测兜底关键逻辑。
- **适用范围**：在本仓库内协助编码、测试、文档与提交准备的 AI；人类协作者可同步阅读并对齐预期。
- **非目标**：替业务做未授权的产品决策；在缺乏依据时扩大改动范围或进行大范围「美化式」重构。

## 2. 文档地图（必读顺序）

1. **`AGENTS.md`（本文件）** — 流程、硬规则、多 Agent 与上下文管理。
2. **`docs/ENGINEERING_CAPABILITIES.md`** — 编码约束、lint/format、目录与构建约定、**依赖与跨包影响**、类型与工具链细节；**自检命令顺序**以 Playbook 与根 `package.json` 为准。可复用能力与包入口见 **`docs/CAPABILITY_MAP.md`**（导航用，**规则**仍以工程能力文档为准）。
3. **`docs/AI_OPERATION_PLAYBOOK.md`** — 从分支、证据检查到门禁命令的**可执行步骤**（与本文件互补，避免总则重复罗列命令细节）。
4. **`docs/PR_TEMPLATE.md`** — PR 描述字段；与下文「任务交付模板」对齐。
5. **（按需）** `docs/AI_ARCHIVING_SPEC.md` 与 `docs/AI_CHANGELOG.md` — AI 变更归档与追溯。
6. **（按需）** `docs/AI_BUGFIX_GUIDE.md` — 缺陷排查与常见问题补充（原则见 `docs/ENGINEERING_CAPABILITIES.md`「Bug 修复与回归策略」）。
7. **（按需）** `docs/reference/README.md` — 工具链参考文档入口（TypeScript / ESLint / Prettier / Build 等）。

## 3. 规则优先级与冲突处理

当用户即时指令与本仓库规范并存时，按以下顺序解释与执行（**就高不就低**）：

1. **安全与合规**：不向仓库写入密钥与敏感信息；日志与对外输出须脱敏；不绕过明显安全边界。
2. **本仓库硬规则**：`AGENTS.md`、`docs/ENGINEERING_CAPABILITIES.md`、可执行配置（`package.json`、`eslint.config.mjs`、`prettier.config.mjs`、`tsconfig`\*、`.ruff.toml` 等）。
3. **用户本次任务的明确范围与验收标准**（在不妨碍 1、2 的前提下）。

若仍存在冲突（例如用户要求删除全部测试或提交密钥），必须**停止并说明原因**，请求人类澄清或调整任务，不得静默执行。

## 4. 固定规则（AI 必须遵守）

全文引用下列条目时，统一写作 **「固定规则第 N 项」**，其中 **N** 与下列序号一致。

1. **先读本文件**，再按第 2 节地图查阅工程细则与 Playbook；开始改代码前完成与任务相关的最小阅读。
2. **工程能力细则**：编码、lint/format、目录边界、框架与 UI 政策等必须遵循 `docs/ENGINEERING_CAPABILITIES.md`。
3. **复核依据**：最终行为以可执行配置为准：`package.json` 脚本、`tsconfig.json`、`eslint.config.mjs`、`prettier.config.mjs`、`.ruff.toml`；构建分工见 `docs/reference/build/README.md`（**Vite** / **tsup**）。
4. **单一事实来源**：任何**新增**工程规则或能力，先写入 `docs/ENGINEERING_CAPABILITIES.md`；本文件只保留协作总纲、入口与稳定条款顺序，避免两处各写一套细节。
5. **提交前本地校验**（具体组合与例外以工程能力文档与 Playbook 为准）：

- 默认：`pnpm lint`、`pnpm format:check`。
- 涉及 TypeScript 业务逻辑、协议、数据结构或关键流程：`pnpm typecheck`、`pnpm test:gate`（仓库中完全不存在匹配测试内容时可按脚本设计跳过）；存在测试内容时补齐或更新至少一个测试文件并执行 `pnpm test:run`。
- 对**新增/修改功能行为**（含新增导出函数、条件分支、边界处理）且目标包尚无测试脚本时：先为该包补齐最小可执行测试脚本与至少一个测试文件，再执行测试；禁止仅用 `typecheck` 代替功能验证。
- 业务变更测试须覆盖主路径及**业务边界与异常/错误路径**（细则见 `docs/ENGINEERING_CAPABILITIES.md`「业务变更测试的内容要求（边界与异常）」）；无法覆盖时须在 PR「风险与后续」说明。
- **任意功能变更**（不限模块/层级）必须提供并落实**测试矩阵**：**必选项**为 **happy path（正确输入）**、**negative path（错误类型）**、**boundary path（边界值测试，必须）**、**runtime path（运行时差异，如全局对象缺失）**；**可选项**为 **等价类划分测试**、**兼容性测试**、**性能测试**；**条件必选项**为 **安全测试（若涉及认证/授权、输入校验、数据访问控制、敏感数据处理或安全边界变更，则必须测试）**。缺任一必选项或条件必选项不得宣称完成；确因上下文限制无法覆盖时，须在 PR「风险与后续」明确缺口、影响范围与补测计划。
- **仅文档变更**（例如仅修改 Markdown、且未改 `apps/` / `packages/` / `portals/` 下业务源码等）：允许按 `docs/AI_OPERATION_PLAYBOOK.md` **§2.7** 精简自检命令，但须在「验证结果」中说明变更范围与已执行命令，且**至少**通过 `pnpm format:check`。
- 涉及 Python 且本地可用：`pnpm ruff`。

6. **分支命名**：用于隔离开发的 AI 分支名必须以 `ai/` 或 `ai-` 开头，便于识别并降低污染主分支的风险。
7. **证据优先**：关键接口、协议、数据结构、边界条件须能在仓库内定位到确定依据；无法定位时**停止**，列出待补充信息，禁止用「可能、大概、猜测」实现关键路径。
8. **文档与注释语言**：叙述以**中文**为主；工具名、配置键、ESLint rule id、API 符号、URL 等**专有名词保留英文**；细则见 `docs/ENGINEERING_CAPABILITIES.md` 中的「文档与注释语言」。
9. **自测后再交付**：在宣称任务完成、建议合并或结束本回合前，须对**本次变更适用**的门禁与校验完成**实际执行**（在具备运行条件的环境中自行运行相关命令），依据输出迭代修复直至通过或明确说明阻塞原因（环境不可用、需人类提供的凭据等）。禁止仅用「理论上应通过」「请本地自行验证」代替自测；**验证结果**须如实反映已执行的命令与结论（见**第 10 节**「任务交付模板」）。具体命令组合见**固定规则第 5 项**与 `docs/AI_OPERATION_PLAYBOOK.md`。迭代修复门禁失败时须同时遵守**固定规则第 10 项**（门禁修复熔断）。
10. **门禁修复熔断**：针对**同一类** `lint` / `format:check` / `typecheck` / `test:gate` / `test:run` / `ruff` 等失败，若已连续 **3** 轮尝试修复仍无法通过，必须**停止**；输出相关错误堆栈或关键报错行，说明当前判断与假设，请求人类干预。**禁止**为通过门禁而进行与根因无关的**大规模重构**或漫无边界改写。人类给出新方向或明确授权继续排查后，可重新开始计数。

## 5. 授权边界与敏感操作

以下类型操作须在执行前取得**人类明确确认**（或任务已书面授权），禁止自作主张：

- 批量删除文件、删除或裁并目录、目录级移除或大范围重命名公共 API。
- 修改 CI/CD、部署脚本、密钥与凭据管理相关路径与约定。
- 修改环境变量约定、`.env*` 模板或示例、以及 CI/部署流程中的密钥与敏感配置注入方式（即使未写入真实密钥值）。
- **major** 级别依赖升级，或引入与现有栈明显冲突的大型依赖。
- 变更影响安全边界、**认证/授权/会话等与身份安全相关的逻辑**、权限模型、多租户隔离或对外协议的语义。

**禁止进入仓库的内容**：token、密码、私钥、未脱敏个人数据、未公开的内网地址等；日志与归档须脱敏，见 `docs/AI_ARCHIVING_SPEC.md`。

## 6. 仓库结构（顶层语义）

- **应用**：`apps/`\*
- **门户 / 独立入口应用**：`portals/`\*（workspace 子包，说明见 `portals/README.md`）
- **共享库**：`packages/`_（可含归类子目录，如 `packages/intl/`_；细则见工程能力文档）
- **规范与手册**：`docs/`\*（含 `reference/` 工具链参考）
- **仓库级脚本**：`scripts/`\*（门禁、文档生成等）
- **工作区**：`pnpm-workspace.yaml`

## 7. 多 Agent 协作协议

多 Agent 指同一任务在流程上由不同**职责阶段**承担（可由同一对话分阶段执行，或由多人/多会话接力）。目标是：**边界清晰、交接可复用、减少重复探索**。

### 7.1 角色与职责

| 角色              | 职责                                                                                                             |
| ----------------- | ---------------------------------------------------------------------------------------------------------------- |
| **Main Agent**    | 拆解任务、维护「任务简报」、汇总输出「变更摘要 + 验证计划 + 风险」；裁决范围与优先级（在总纲与工程文档框架内）。 |
| **Explore Agent** | 只读探索：定位实现位置、依赖边界、可复用点、风险面；**不**顺带做大范围重构。                                     |
| **Edit Agent**    | 按证据与范围实现修改；小步、可回滚；遵守工程能力文档。                                                           |
| **Test Agent**    | 补/改测试，运行门禁与测试命令，定位失败原因并反馈最小复现信息。                                                  |
| **Doc Agent**     | 更新变更说明、PR 片段与必要注释；与 `docs/PR_TEMPLATE.md` 对齐。                                                 |

### 7.2 推荐执行顺序

1. **Main**：写清目标、非目标、目录范围、验收标准（可引用用户原话）。
2. **Explore**：输出结构化结果（见下节「交接物」），未经探索不得盲改关键路径。
3. **Edit**：仅在 Explore 结论与证据充分时进入；若发现依据不足，退回 Explore 或停止澄清。
4. **Test**：实现与测试断言同步或在实现后立即补齐，按**固定规则第 5 项、第 9 项、第 10 项**与 Playbook 跑校验。
5. **Doc**：定稿前补齐 PR 描述与归档条目（若适用）。

允许在**小任务**上合并 Explore + Edit（同一执行者须自行完成证据检查）；**大任务或高风险域**建议固定顺序并保留 Explore 产出。

### 7.3 交接物（Handoff）—— 强制最小集

任一 Agent 向下游交接时，应提供**短且可检索**的摘要，避免粘贴大段代码或全文日志：

- **目标与范围**：涉及的包路径（如 `packages/foo`、`apps/bar`），明确「不做什么」。
- **已确认事实**：文件/符号路径、既有类型或 API 名称、调用约定（**引用路径与标识符，不贴全文**）。
- **待决问题**：缺失的协议/产品决策（若有）。
- **风险与假设**：兼容性、数据、安全相关；标注「已验证 / 待验证」。
- **下游指令**：下一角色具体要产出的工件（例如「补 `*.test.ts` 覆盖分支 X」）。

Main Agent 负责把上述内容收敛进最终「任务交付模板」。

## 8. 上下文管理（最大化有效信息、控制体积）

1. **先收窄再读取**：用搜索、索引、`glob`、语义检索定位候选文件，再打开少量文件精读；禁止无范围地通读大目录。
2. **优先仓库内证据**：类型定义、现有测试、调用方、错误处理惯例优于外部猜测；`docs/reference/` 用于查配置选项，不替代业务协议来源。
3. **单任务单焦点**：一次会话优先完成一个可交付增量；范围膨胀时由 Main 拆分为子任务并排队。
4. **引用代替复述**：交接时使用「文件路径 + 符号名 + 行号范围（若已知）」，避免重复粘贴已有实现。
5. **长输出折叠策略**：详细命令日志、大段 diff 可放入 PR/附件或摘要「通过/失败 + 关键错误行」，不必在对话中重复全文。
6. **与子 Agent / 工具协作**：委派探索类任务时给出**明确检索目标与返回格式**（例如「列出 `packages/request` 中所有导出 HTTP 客户端的位置」），避免空泛提问浪费往返。

### 8.1 动态上下文加载策略（建议）

在 Context Window 有限的前提下，**优先少而准**：先读本文件与**第 2 节文档地图**，再按任务范围**增量**打开下列材料；**须**在工具中实际读取或引用具体段落，**禁止**仅凭路径名「假装已读」。

| 任务侧重                                             | 建议优先加载（在完整阅读本文件之后）                                                                                                                                           |
| ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 仅 UI / 页面展示与样式（不涉及协议、共享包对外 API） | `docs/ENGINEERING_CAPABILITIES.md` 中「框架与 UI 组件优先复用」「AI 与设计决策边界」；交付与门禁仍须对照 `docs/AI_OPERATION_PLAYBOOK.md` **§2.3**（仅文档变更时 **§2.7**）     |
| 改动 `packages/*` 或 workspace 共享包                | **`docs/CAPABILITY_MAP.md`**（包入口一览）；`docs/ENGINEERING_CAPABILITIES.md` 中「目录与模块边界」**跨包影响与审计**、「依赖与第三方库」；对外 API/导出另见 Playbook **§2.2** |
| 业务逻辑 / 协议 / 数据结构 / 测试门禁                | `docs/ENGINEERING_CAPABILITIES.md` 中「TypeScript 优先」「业务变更测试」等；`docs/AI_OPERATION_PLAYBOOK.md` **§2.3～§2.4**                                                     |
| 仅 Markdown / 规范文档，未改业务源码                 | Playbook **§2.7**；与 `AGENTS.md` / `docs` 冲突时仍以工程能力文档为准                                                                                                          |
| 范围不清或高风险域                                   | 宁可多打开 `docs/ENGINEERING_CAPABILITIES.md` 并**搜索关键词**，或向人类确认范围，**不得**用猜测补齐未读条文                                                                   |

**说明**：`docs/CAPABILITY_MAP.md` 体量小，涉及 `packages/*` 时**应尽快打开**，避免凭记忆推断包名与路径。

## 9. 总体工作流（每次任务）

1. **澄清**：输入、输出、影响目录（`apps/*` / `portals/*` / `packages/*`）、验收标准。
2. **探索**：对齐命名、模块边界与可复用点（见第 7、8 节）。
3. **实现**：按证据小步修改；每步完成后按 Playbook 做本地自检（见**固定规则第 9 项**）。
4. **自测闭环**：交付前对适用范围内的 `lint` / `format:check` / `typecheck` / `test:gate` / `test:run` / `ruff` 等完成实际执行，失败则修复后再汇总；不得跳过自测环节（**固定规则第 9 项**）；多轮仍失败时遵守**固定规则第 10 项**。
5. **克制改动**：禁止为「好看」进行无关重构；无关文件不手滑修改。
6. **风险可见**：对可能影响外部的行为变更，按兼容性、安全、性能、可靠性、数据一致性做简短评估，并给出缓解与验证方式（见**第 10 节**「任务交付模板」）。

## 10. 任务交付模板（Main Agent 汇总时必须包含）

以下内容与 `docs/PR_TEMPLATE.md` 字段对齐，便于一次复制到 PR：

- **变更摘要**：做了什么、为什么做。
- **影响面**：接口 / 协议 / 行为变化；若无则明确写「无」。
- **克制性说明（Refrained from）**：为遵守**最小化改动**（见第 9 节「克制改动」、工程文档与 Playbook），**本可**做但未做的重构、美化或扩大范围之举，及**不做的原因**（如超出本次范围、回归风险）。无则写「无」。
- **测试矩阵**：按功能点列出本次已覆盖的**必选项（happy path / negative path / boundary path / runtime path）**、**可选项（等价类划分 / 兼容性 / 性能）**与**条件必选项（安全测试，若适用）**；若有缺口，必须给出原因、风险等级、影响范围与补测计划。
- **验证结果**：执行了哪些命令（如 `pnpm lint`、`pnpm format:check`、`pnpm typecheck`、`pnpm test:gate`、`pnpm test:run`、`pnpm ruff` 等）、结论与必要说明。
- **验证证据格式**：回复中必须包含「已实际执行命令清单 + 每条命令结论（通过/失败）」。若失败，需给出关键报错与下一步处理；不得仅写「已验证」「理论可通过」。
- **风险与后续**：风险清单（含严重度）、触发条件与受影响范围、缓解与验证方式、后续跟踪建议。

## 11. 延伸阅读

| 文档                               | 说明                             |
| ---------------------------------- | -------------------------------- |
| `docs/ENGINEERING_CAPABILITIES.md` | 工程规则与工具链约定             |
| `docs/CAPABILITY_MAP.md`           | 仓库工程能力地图（包与规则入口） |
| `docs/AI_OPERATION_PLAYBOOK.md`    | AI 执行步骤与自检命令            |
| `docs/AI_BUGFIX_GUIDE.md`          | Bug 排查补充（按需）             |
| `docs/PR_TEMPLATE.md`              | PR 描述模板                      |
| `docs/AI_ARCHIVING_SPEC.md`        | AI 变更归档规范                  |
| `docs/AI_CHANGELOG.md`             | 归档条目落地                     |
| `docs/reference/README.md`         | 工具链参考索引                   |
