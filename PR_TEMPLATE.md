# PR 说明

## 变更摘要

<!-- 做了什么、为什么做 -->

## 影响面

<!-- 涉及的接口/协议/行为变化；涉及哪些 `apps/*` / `portals/*` / `packages/*` 模块 -->

<!-- 若有设计稿、需求/issue、设计系统说明等依据，可写可访问链接或仓库内文档路径（须脱敏）；无则省略。视觉与信息架构边界见 `docs/ENGINEERING_CAPABILITIES.md`「AI 与设计决策边界」。 -->

## 克制性说明（Refrained from）

<!-- 与 `AGENTS.md` 第 10 节对齐：为最小化改动，本可做但未做的重构/美化/扩范围及原因；无则写「无」。示例：本想将某 Class 改为 Hooks，因超出本次 Bug 范围且降低回归风险而保持原状。 -->

## 测试矩阵

<!-- 与 `AGENTS.md` 固定规则第 5 项、第 10 节对齐。 -->
<!-- 必选：happy path / negative path / boundary path / runtime path。 -->
<!-- 可选：等价类划分 / 兼容性 / 性能。 -->
<!-- 条件必选：安全测试（涉及认证/授权、输入校验、访问控制、敏感数据处理或安全边界变更时必须）。 -->
<!-- 若缺口存在，需说明原因、风险等级、影响范围与补测计划。 -->

- 必选覆盖：
  - [ ] happy path（正确输入）
  - [ ] negative path（错误类型）
  - [ ] boundary path（边界值：`null` / `undefined` / 空值 / 极端值）
  - [ ] runtime path（运行时差异，如全局对象缺失）
- 可选覆盖（按收益补充）：
  - [ ] 等价类划分测试
  - [ ] 兼容性测试
  - [ ] 性能测试
- 条件必选：
  - [ ] 安全测试（若适用）
- 缺口说明（若有）：<!-- 原因 / 风险等级 / 影响范围 / 补测计划 -->

## 验证结果（PR 前必须先通过）

<!-- 与 `AGENTS.md` 固定规则第 5、9、10 项、`docs/AI_OPERATION_PLAYBOOK.md` §2.3 对齐；未跑某条须写明原因或「待验证」。同一类门禁失败已连续多轮仍无法修复时，须按第 10 项停止并说明，不得虚报为已通过。仅文档变更见 Playbook §2.7。 -->

- [ ] `pnpm lint`
- [ ] `pnpm format:check`
- [ ] `pnpm typecheck`（涉及 TS/业务逻辑/协议/数据结构或关键流程时必跑）
- [ ] `pnpm test:gate`（业务变更须跑；无业务源码变更时多为通过/跳过）
- [ ] `pnpm test:run`（存在需执行的测试时）
- [ ] `pnpm ruff`（涉及 Python 且本地可用时；CI 可能单独跑）
- [ ] `pnpm build`（可选：改动影响构建产物、对外入口或子包 `exports` 时建议跑并在此说明结论）

## 风险与后续

<!-- 按风险维度给出：风险清单（含严重度）+ 触发条件/受影响范围 + 缓解/验证方式 + 下一步跟踪建议 -->

## 关联

<!-- 关联 issue/需求/PR/commit hash（如有） -->
